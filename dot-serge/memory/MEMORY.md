# Serge memory — index

Durable facts that survive across sessions. One line per fact, newest first,
each linking a small file in this directory. Serge reads the linked files on
demand — it does not load them all, so this index must stay terse.

Write rules live in the `## memory` section of `../CONSTITUTION.md`.

## Facts

<!-- (empty — Serge fills this in as it learns) -->
