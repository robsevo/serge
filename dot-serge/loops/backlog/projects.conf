# Projects the autonomous backlog loop is allowed to work on.
# One absolute path per line. Empty = the loop does nothing, which is the
# default on purpose: an autonomous loop pointed at a repo you did not intend
# is the single most expensive misconfiguration in this system.
# Read loops/README.md before enabling any loop.
