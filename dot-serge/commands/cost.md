---
name: cost
description: Show Serge's quota fuel gauge (per-provider daily usage — the real currency on the all-free roster) plus any paid-hatch spend
allowed-tools: Bash($SERGE_HOME/cost.sh:*)
---

Run `$SERGE_HOME/cost.sh` and show its output to the user exactly as printed. Add nothing — no preamble, no analysis, no follow-up offer. Just the gauge.
